{
  "name": "@mono/driver",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build:bundle": "node build.mjs",
    "build:linux": "pnpm build:bundle && node build-linux.mjs",
    "build:sea": "node build-sea.mjs",
    "build": "pnpm build:bundle && pnpm build:sea",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@mono/api": "workspace:*",
    "zod": "4.3.5"
  },
  "devDependencies": {
    "@types/node": "25.0.9",
    "esbuild": "0.25.1",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vitest": "^4.0.18"
  }
}
