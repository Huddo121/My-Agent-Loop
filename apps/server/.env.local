NODE_ENV=development
DATABASE_URL=postgres://my_agent_loop:my_agent_loop@localhost:5432/my_agent_loop
REDIS_HOST=localhost
APP_BASE_URL=http://localhost:5173
# DRIVER_HOST_API_BASE_URL defaults to http://host.docker.internal:${PORT}
# MCP_SERVER_URL defaults to http://host.docker.internal:3050/mcp

# Generate a random key with `openssl rand -base64 32`
# FORGE_ENCRYPTION_KEY=...

# Generate a random key with `openssl rand -base64 32`
# BETTER_AUTH_SECRET=...
